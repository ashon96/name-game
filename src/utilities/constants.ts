/**
 * The conversion factor used to convert
 * milliseconds to seconds
 */
export const SECONDS_CONVERSION = 1000;

/**
 * The number of rounds a user plays
 * in one game
 */
export const NUMBER_OF_ROUNDS = 5;

/**
 * The conversion factor used to convert
 * a fraction to a percentage
 */
export const PERCENTAGE_CONVERSION = 100;

/**
 * A profile image's set height and width
 */
export const IMAGE_DIMENSION = 260;
